degree:year:branch
x for all diff x we shud have diff file
name accordingly
function to create batchwise csv files and append data to them

each dict is one course with code, coursenaame, slot (as keys)
[{
    code, coursenaame, slot (as keys)

},

{

}
...]
def getData(deg, sem, branch):
    sample = ""
    csvlist = []#add all csv files from folder
    for file in csvlist:
        if file == sample:
            #extract code, name , slot
            return [{code:[name,slot]},{},{}]
def getTiming(dict array):